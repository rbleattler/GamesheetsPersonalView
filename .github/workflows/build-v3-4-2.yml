name: BuildV342
on:
  push:
    branches:
      - main
    paths:
      - tools/build_v3_4_2.py
      - .github/workflows/build-v3-4-2.yml
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Build
        run: python3 tools/build_v3_4_2.py
      - name: Validate
        run: |
          python3 - <<'PY'
          import re
          from pathlib import Path
          s=Path('v3.4.2.html').read_text(encoding='utf-8')
          Path('/tmp/app.js').write_text('\n'.join(re.findall(r'<script>(.*?)</script>',s,re.S)),encoding='utf-8')
          assert 'playerPickerSelection' in s
          assert 'Add Players' in s
          assert 'V3.4.2' in s
          PY
          node --check /tmp/app.js
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add v3.4.2.html gamesheets_plus.html index.html
          git commit -m 'feat: clarify Add Players selection flow'
          git push
