{
  "name": "myhockeyhub",
  "private": true,
  "version": "4.0.0-beta.0",
  "type": "module",
  "scripts": {
    "check": "node --check scripts/build.mjs && node --check scripts/verify-build.mjs && node --check src/app/router.js && node --check src/app/foundation.js && node --check src/app/diagnostics.js && node --check tests/foundation.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
